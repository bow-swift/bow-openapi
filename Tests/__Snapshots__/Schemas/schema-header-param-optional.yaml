openapi: "3.0.0"
paths:
  /pet:
    get:
      operationId: getPet
      parameters:
        - name: pet-Id-Testing
          in: header
          required: false
          schema:
            type: integer
